# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

All repository guidance lives in `AGENTS.md` so it can be shared by every coding agent (Claude Code, Codex, Cursor, Copilot, etc.). Claude Code auto-loads the file referenced below at session start.

@AGENTS.md
